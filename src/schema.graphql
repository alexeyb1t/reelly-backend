schema {
    query: Query
}

type Query {
    projectsSearch(projectsSearchInput: ProjectsSearchInput!): Project
}

type Project {
    id: String!
    name: String!
    bedrooms: Int!
    region: String!
}

input ProjectsSearchInput {
    name: String!
    bedrooms: Int!
    region: String!
}
